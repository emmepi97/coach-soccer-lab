import { useEffect, useState } from 'react';
import StatCard from './Shared/StatCard';
import { getPlayers } from '../services/playersService';
import { getObservations } from '../services/observationsService';
import { getSeasons } from '../services/seasonsService';
import { getMatches } from '../services/matchesService';
import { getAnalyticsEvents, summarizeAnalytics } from '../services/analyticsService';
export default function Dashboard({activeSeason,setActive}){ const [s,setS]=useState({}); useEffect(()=>{ async function run(){ const [players,obs,seasons]=await Promise.all([getPlayers(),getObservations(),getSeasons()]); let matches=[], events=[]; if(activeSeason){ matches=await getMatches(activeSeason.id); events=await getAnalyticsEvents(activeSeason.id); } setS({players,obs,seasons,matches,events,analytics:summarizeAnalytics(events)}); } run().catch(()=>{}); },[activeSeason]); const wins=(s.matches||[]).filter(m=>m.esito==='vittoria').length, draws=(s.matches||[]).filter(m=>m.esito==='pareggio').length, losses=(s.matches||[]).filter(m=>m.esito==='sconfitta').length; return <div className="section"><div className="hero"><h1>Dashboard {activeSeason?`- ${activeSeason.name}`:''}</h1><p>Controllo rapido di scouting, rosa, risultati, presenze, analytics e IPO.</p><div className="quick"><button onClick={()=>setActive('observations')}>+ Osservazione</button><button onClick={()=>setActive('results')}>+ Risultato</button><button onClick={()=>setActive('attendance')}>+ Presenza</button><button onClick={()=>setActive('analytics')}>+ Evento analytics</button></div></div><div className="statsGrid"><StatCard label="Giocatori database" value={s.players?.length}/><StatCard label="Osservazioni" value={s.obs?.length}/><StatCard label="Stagioni condivise/attive" value={s.seasons?.length}/><StatCard label="Partite" value={s.matches?.length}/><StatCard label="Record V/P/S" value={`${wins}/${draws}/${losses}`}/><StatCard label="Eventi analytics" value={s.events?.length}/><StatCard label="IPO nostro" value={s.analytics?.ipo_nostro}/><StatCard label="IPO avversario" value={s.analytics?.ipo_avversario}/></div></div>; }
