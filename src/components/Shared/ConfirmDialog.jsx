import Modal from './Modal'; export default function ConfirmDialog({open,title='Conferma',message,onConfirm,onClose}){ return <Modal open={open} title={title} onClose={onClose}><p className="muted">{message}</p><div className="formActions"><button className="danger" onClick={onConfirm}>Conferma eliminazione</button><button className="secondary" onClick={onClose}>Annulla</button></div></Modal>; }
