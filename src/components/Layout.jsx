import { APP_NAME } from '../lib/supabaseClient';
import { signOut } from '../services/authService';
const items=[['dashboard','Dashboard'],['players','Database Giocatori'],['observations','Osservazioni Mercato'],['seasons','Stagioni'],['members','Staff / Membri'],['results','Risultati'],['attendance','Presenze'],['roster','Rosa'],['analytics','Analytics'],['ipo','Eventi IPO'],['excel','Excel'],['settings','Impostazioni']];
export default function Layout({active,setActive,children,profile,planCode,isOwner,activeSeason}){
 return <div className="appShell"><aside><div className="brand"><span>⚽</span><strong>{APP_NAME}</strong></div>{activeSeason&&<div className="seasonPill"><small>Stagione attiva</small><b>{activeSeason.name}</b><span>{activeSeason.team_name}</span></div>}<nav>{items.map(([k,l])=><button key={k} className={active===k?'active':''} onClick={()=>setActive(k)}>{l}</button>)}{isOwner&&<button className={active==='owner'?'active ownerBtn':'ownerBtn'} onClick={()=>setActive('owner')}>Owner Admin</button>}</nav><div className="sideFooter"><small>{profile?.email}</small><b>Piano: {planCode}</b><button className="ghost" onClick={signOut}>Logout</button></div></aside><section className="content">{children}</section></div>;
}
