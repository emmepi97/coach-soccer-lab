export default function MonetizationPanel({plans=[]}){ return <div><h2>Monetizzazione</h2><p>Stripe non è integrato in questa prima versione, ma la struttura è pronta per Checkout, Customer Portal e webhook.</p><div className="cardGrid">{plans.map(p=><div className="card" key={p.plan_code}><h3>{p.plan_name}</h3><p>{p.description}</p><strong>{p.monthly_price}€/mese · {p.yearly_price}€/anno</strong></div>)}</div><button className="secondary">Configura Stripe (placeholder)</button></div>; }
